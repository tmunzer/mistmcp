searchOrgDevice:
  type: tool_replacement
  tags: [devices]
  read_only_hint: true
  destructive_hint: false
  description: Search a network device in the Organization Inventory. This tool provides a consolidated view of all devices within an organization, even those not assigned to any site. This can be used to quickly search for a device across the whole organization. It allows filtering by various attributes such as serial number, model, MAC address, firmware version, device type, and connection status. This tool is useful for quickly finding specific devices or getting an overview of the organization's inventory without needing to query each site separately.
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to filter inventory by
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to filter inventory by
    - name: serial
      schema:
        type: string
      description: Serial number of the device to filter inventory by
    - name: model
      schema:
        type: string
      description: Model of the device to filter inventory by
    - name: mac
      schema:
        type: string
      description: MAC address of the device to filter inventory by
    - name: version
      schema:
        type: string
      description: Firmware version of the device to filter inventory by
    - name: vc_mac
      schema:
        type: string
      description: MAC address of the virtual chassis (switch stack) to filter inventory by
    - name: device_type
      schema:
        type: string
        enum: [ap, switch, gateway]
      description: Type of the device to filter inventory by
    - name: status
      schema:
        type: string
        enum: [connected, disconnected]
      description: Connection status of the device to filter inventory by
    - name: text
      schema:
        type: string
      description: Text to search for in device attributes (name, serial, MAC). Use the wildcard `*` for partial matches (e.g. `AP*` to match all devices with names starting with "AP")
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  request:
    operationId: searchOrgInventory
    function: mistapi.api.v1.orgs.inventory.searchOrgInventory(apisession,org_id=str(org_id),serial=serial if serial else None,model=model if model else None,type=device_type.value if device_type else None,mac=mac if mac else None,site_id=str(site_id) if site_id else None,vc_mac=vc_mac if vc_mac else None,version=version if version else None, text=text if text else None, status=status.value if status else None,limit=limit,)

listSiteWirelessClientsStats:
  type: tool_optimization
  add_parameters:
    - name: client_mac
      schema:
        type: string
      description: MAC address of the client to filter stats by
  custom_request: mistapi.api.v1.sites.stats.getSiteWirelessClientStats(apisession, site_id=str(site_id), client_mac=client_mac)
getSiteWirelessClientStats:
  skip: true

listSiteRogueAPs:
  type: tool_optimization
  add_parameters:
    - name: rogue_bssid
      schema:
        type: string
      description: BSSID of the rogue AP to filter stats by
  custom_request: mistapi.api.v1.sites.rogues.getSiteRogueAP(apisession, site_id=str(site_id), rogue_bssid=rogue_bssid)
getSiteRogueAP:
  skip: true

listUpgrades:
  type: tool_consolidation
  tags: [utilities_upgrade]
  description: Retrieve upgrade-related information for the organization. Use device types (ap, switch, srx, mxedge, ssr) to list or retrieve upgrade jobs. Use available_device_versions to list available firmware versions for AP/switch/gateway devices. Use available_ssr_versions to list available SSR firmware versions.
  read_only_hint: true
  destructive_hint: false
  match_name: device_type
  if_filter: upgrade_id
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization
      required: true
    - name: device_type
      schema:
        type: string
        enum:
          [
            ap,
            switch,
            srx,
            mxedge,
            ssr,
            available_device_versions,
            available_ssr_versions,
          ]
      required: true
      description: "Type of query: use ap/switch/srx/mxedge/ssr to list upgrade jobs for that device type; use available_device_versions to list available firmware versions for AP/switch/gateway devices; use available_ssr_versions to list available SSR firmware versions"
    - name: upgrade_id
      schema:
        type: string
        format: uuid
      description: ID of a specific upgrade job to retrieve. Only applicable when device_type is ap, switch, srx, mxedge, or ssr
      only_if:
        device_type:
          - ap
          - switch
          - srx
          - mxedge
          - ssr
    - name: firmware_type
      schema:
        type: string
        enum: [ap, switch, gateway]
      description: Device model type to filter available firmware versions by. Only applicable when device_type is available_device_versions
      only_if:
        device_type:
          - available_device_versions
    - name: model
      schema:
        type: string
      description: Device model to filter available firmware versions by. Only applicable when device_type is available_device_versions
      only_if:
        device_type:
          - available_device_versions
    - name: channel
      schema:
        type: string
        enum: [alpha, beta, stable]
      description: SSR firmware release channel to filter by. Only applicable when device_type is available_ssr_versions. Defaults to stable
      only_if:
        device_type:
          - available_ssr_versions
    - name: mac
      schema:
        type: string
      description: MAC address (or comma-separated list) of SSR device(s) to retrieve available versions for. Only applicable when device_type is available_ssr_versions
      only_if:
        device_type:
          - available_ssr_versions
  requests:
    ap:
      list:
        operationId: listOrgDeviceUpgrades
        function: mistapi.api.v1.orgs.devices.listOrgDeviceUpgrades(apisession, org_id=str(org_id))
      get:
        operationId: getOrgDeviceUpgrade
        function: mistapi.api.v1.orgs.devices.getOrgDeviceUpgrade(apisession, org_id=str(org_id), upgrade_id=str(upgrade_id))
    switch:
      list:
        operationId: listOrgDeviceUpgrades
        function: mistapi.api.v1.orgs.devices.listOrgDeviceUpgrades(apisession, org_id=str(org_id))
      get:
        operationId: getOrgDeviceUpgrade
        function: mistapi.api.v1.orgs.devices.getOrgDeviceUpgrade(apisession, org_id=str(org_id), upgrade_id=str(upgrade_id))
    srx:
      list:
        operationId: listOrgDeviceUpgrades
        function: mistapi.api.v1.orgs.devices.listOrgDeviceUpgrades(apisession, org_id=str(org_id))
      get:
        operationId: getOrgDeviceUpgrade
        function: mistapi.api.v1.orgs.devices.getOrgDeviceUpgrade(apisession, org_id=str(org_id), upgrade_id=str(upgrade_id))
    mxedge:
      list:
        operationId: listOrgMxEdgeUpgrades
        function: mistapi.api.v1.orgs.mxedges.listOrgMxEdgeUpgrades(apisession, org_id=str(org_id))
      get:
        operationId: getOrgMxEdgeUpgrade
        function: mistapi.api.v1.orgs.mxedges.getOrgMxEdgeUpgrade(apisession, org_id=str(org_id), upgrade_id=str(upgrade_id))
    ssr:
      list:
        operationId: listOrgSsrUpgrades
        function: mistapi.api.v1.orgs.ssr.listOrgSsrUpgrades(apisession, org_id=str(org_id))
      get:
        operationId: getOrgSsrUpgrade
        function: mistapi.api.v1.orgs.ssr.getOrgSsrUpgrade(apisession, org_id=str(org_id), upgrade_id=str(upgrade_id))
    available_device_versions:
      list:
        operationId: listOrgAvailableDeviceVersions
        function: mistapi.api.v1.orgs.devices.listOrgAvailableDeviceVersions(apisession, org_id=str(org_id), type=firmware_type.value if firmware_type else None, model=model if model else None)
    available_ssr_versions:
      list:
        operationId: listOrgAvailableSsrVersions
        function: mistapi.api.v1.orgs.ssr.listOrgAvailableSsrVersions(apisession, org_id=str(org_id), channel=channel.value if channel else "stable", mac=mac if mac else None)

getInsightMetrics:
  type: tool_consolidation
  tags: [sites_insights]
  description: Get insight metrics for a given object
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the Mist Site
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of object to retrieve metrics for
      required: true
    - name: metric
      schema:
        type: string
      description: Name of the metric to retrieve. Use the tool`mist_get_constants` with `object_type=insight_metrics` to see available metrics
      required: true
    - name: mac
      schema:
        type: string
      description: MAC address of the client or device to retrieve metrics for. Required if object_type is 'client', 'ap', 'mxedge' or 'switch'
      required: false
    - name: device_id
      schema:
        type: string
        format: uuid
      description: ID of the gateway device to retrieve metrics for. Required if object_type is 'gateway'
      required: false
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: duration
      schema:
        type: string
      description: Duration like 7d, 2w
      required: false
    - name: interval
      schema:
        type: string
      description: Aggregation works by giving a time range plus interval (e.g. 1d, 1h, 10m) where aggregation function would be applied to
      required: false
    - name: page
      schema:
        type: integer
      description: Page number
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    site:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetrics(apisession, site_id=str(site_id), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

    client:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetricsForClient(apisession, site_id=str(site_id), client_mac=str(mac), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

    ap:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetricsForDevice(apisession, site_id=str(site_id), device_mac=str(mac), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

    gateway:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetricsForGateway(apisession, site_id=str(site_id), device_id=str(device_id), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

    mxedge:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetricsForMxEdge(apisession, site_id=str(site_id), device_mac=str(mac), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

    switch:
      get:
        operationId: getSiteInsightMetrics
        function: mistapi.api.v1.sites.insights.getSiteInsightMetricsForSwitch(apisession, site_id=str(site_id), device_mac=str(mac), metric=str(metric), start=str(start), end=str(end), duration=str(duration), interval=str(interval), limit=limit, page=page)

getConstants:
  type: tool_consolidation
  tags: [constants]
  description: Retrieve Mist platform constants including insight metrics, webhook topics, alarm definitions, device models, events definitions, and license types. Use this to understand available options and configurations for the Mist API.
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: object_type
      schema:
        type: string
      description: "Type of constant to retrieve: fingerprint_types, insight_metrics, license_types, webhook_topics, device_models, device_events, mxedge_models, alarm_definitions, client_events, mxedge_events, or nac_events"
      required: true

  requests:
    fingerprint_types:
      get:
        operationId: listFingerprintTypes
        function: mistapi.api.v1.const.fingerprint_types.listFingerprintTypes(apisession)

    insight_metrics:
      get:
        operationId: listInsightMetrics
        function: mistapi.api.v1.const.insight_metrics.listInsightMetrics(apisession)

    license_types:
      get:
        operationId: listLicenseTypes
        function: mistapi.api.v1.const.license_types.listLicenseTypes(apisession)

    webhook_topics:
      get:
        operationId: listWebhookTopics
        function: mistapi.api.v1.const.webhook_topics.listWebhookTopics(apisession)

    device_models:
      get:
        operationId: listDeviceModels
        function: mistapi.api.v1.const.device_models.listDeviceModels(apisession)

    device_events:
      get:
        operationId: listDeviceEventsDefinitions
        function: mistapi.api.v1.const.device_events.listDeviceEventsDefinitions(apisession)

    mxedge_models:
      get:
        operationId: listMxEdgeModels
        function: mistapi.api.v1.const.mxedge_models.listMxEdgeModels(apisession)

    alarm_definitions:
      get:
        operationId: listAlarmDefinitions
        function: mistapi.api.v1.const.alarm_defs.listAlarmDefinitions(apisession)

    client_events:
      get:
        operationId: listClientEventsDefinitions
        function: mistapi.api.v1.const.client_events.listClientEventsDefinitions(apisession)

    mxedge_events:
      get:
        operationId: listMxEdgeEventsDefinitions
        function: mistapi.api.v1.const.mxedge_events.listMxEdgeEventsDefinitions(apisession)

    nac_events:
      get:
        operationId: listNacEventsDefinitions
        function: mistapi.api.v1.const.nac_events.listNacEventsDefinitions(apisession)

getSiteSle:
  type: tool_consolidation
  tags: [sles]
  description: Provides Information about the Service Level Expectations (SLEs) for a given site. The SLEs are derived from the insight metrics and can be used to monitor the network user experience of the site against the defined SLEs
  read_only_hint: true
  destructive_hint: false
  match_name: object_type
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the Mist Site
      required: true
    - name: scope
      schema:
        type: string
        enum: [client, ap, gateway, mxedge, switch, site]
      description: Scope of the SLEs to retrieve. Can be 'client', 'ap', 'gateway', 'mxedge', 'switch' or 'site'
      required: true
    - name: scope_id
      schema:
        type: string
        description: ID of the object to retrieve SLEs for. Required if scope is 'client', 'ap', 'gateway', 'mxedge' or 'switch'. Optional if scope is 'site'
      required: true
    - name: metric
      schema:
        type: string
      description: Name of the metric to retrieve SLEs for. Use the tool`mist_get_constants` with `object_type=insight_metrics` to see available metrics
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of object to retrieve metrics for
      required: true
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: classifier
      schema:
        type: string
      description: Classifier name. Required when object_type is 'classifier_summary_trend'
      required: false
      required_if:
        test:
          - classifier_summary_trend
    - name: duration
      schema:
        type: string
      description: Duration like 7d, 2w
      required: false

  requests:
    summary:
      get:
        operationId: getSiteSleSummary
        function: mistapi.api.v1.sites.sle.getSiteSleSummary(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impact_summary:
      get:
        operationId: getSiteSleImpactSummary
        function: mistapi.api.v1.sites.sle.getSiteSleImpactSummary(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    summary_trend:
      get:
        operationId: getSiteSleSummaryTrend
        function: mistapi.api.v1.sites.sle.getSiteSleSummaryTrend(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_applications:
      get:
        operationId: listSiteSleImpactedApplications
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedApplications(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_aps:
      get:
        operationId: listSiteSleImpactedAps
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedAps(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_gateways:
      get:
        operationId: listSiteSleImpactedGateways
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedGateways(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_interfaces:
      get:
        operationId: listSiteSleImpactedInterfaces
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedInterfaces(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_switches:
      get:
        operationId: listSiteSleImpactedSwitches
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedSwitches(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_wireless_clients:
      get:
        operationId: listSiteSleImpactedWirelessClients
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedWirelessClients(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_wired_clients:
      get:
        operationId: listSiteSleImpactedWiredClients
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedWiredClients(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    impacted_chassis:
      get:
        operationId: listSiteSleImpactedChassis
        function: mistapi.api.v1.sites.sle.listSiteSleImpactedChassis(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    histogram:
      get:
        operationId: getSiteSleHistogram
        function: mistapi.api.v1.sites.sle.getSiteSleHistogram(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    classifier_summary_trend:
      get:
        operationId: getSiteSleClassifierSummaryTrend
        function: mistapi.api.v1.sites.sle.getSiteSleClassifierSummaryTrend(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,classifier=classifier,start=str(start) if start else None,end=str(end) if end else None,duration=duration if duration else None,)

    threshold:
      get:
        operationId: getSiteSleThreshold
        function: mistapi.api.v1.sites.sle.getSiteSleThreshold(apisession,site_id=str(site_id),scope=scope.value,scope_id=scope_id,metric=metric,)

updateSiteConfigurationObjects:
  type: tool_consolidation
  tags: [write]
  description: |-
    Update or create configuration object for a specified site.\n
    IMPORTANT:\n
    To ensure that you are not missing any existing attributes when updating the configuration object, make sure to :\n
    1. retrieve the current configuration object using the tools `mist_get_configuration_objects` to retrieve the object defined at the site level\n
    2. Modify the desired attributes \n
    3. Use this tool to update the configuration object with the modified attributes\n
    \n
    When creating a new configuration object, make sure to use the`mist_get_configuration_object_schema` tool to discover the attributes of the configuration object and which of them are required
  read_only_hint: false
  destructive_hint: false
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to create or update configuration objects for
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of configuration object to create or update
      required: true
    - name: payload
      schema:
        type: object
      description: JSON payload of the configuration object to update or create. When updating an existing object, make sure to include all required attributes in the payload. It is recommended to first retrieve the current configuration object using the`mist_get_configuration_objects` tool and use the retrieved object as a base for the payload, modifying only the desired attributes
      required: true
    - name: object_id
      schema:
        type: string
        format: uuid
      description: ID of the specific configuration object to update. Optional, if not provided, a new configuration object will be created with the provided payload
      required: false
  requests:
    devices:
      update:
        operationId: updateSiteDevice
        function: mistapi.api.v1.sites.devices.updateSiteDevice(apisession, site_id=str(site_id), device_id=str(object_id), body=payload)

    evpn_topologies:
      update:
        operationId: updateSiteEvpnTopologies
        function: mistapi.api.v1.sites.evpn_topologies.updateSiteEvpnTopology(apisession, site_id=str(site_id), evpn_topology_id=str(object_id), body=payload)
      create:
        operationId: createSiteEvpnTopology
        function: mistapi.api.v1.sites.evpn_topologies.createSiteEvpnTopology(apisession, site_id=str(site_id), body=payload)

    # mxedges:
    #   update:
    #     operationId: updateSiteMxEdge
    #     function: mistapi.api.v1.sites.mxedges.updateSiteMxEdge(apisession, site_id=str(site_id), mxedge_id=str(object_id), body=payload)
    #   create:
    #     operationId: createSiteMxEdge
    #     function: mistapi.api.v1.sites.mxedges.createSiteMxEdge(apisession, site_id=str(site_id), body=payload)

    psks:
      update:
        operationId: updateSitePsk
        function: mistapi.api.v1.sites.psks.updateSitePsk(apisession, site_id=str(site_id), psk_id=str(object_id), body=payload)
      create:
        operationId: createSitePsk
        function: mistapi.api.v1.sites.psks.createSitePsk(apisession, site_id=str(site_id), body=payload)

    webhooks:
      update:
        operationId: updateSiteWebhook
        function: mistapi.api.v1.sites.webhooks.updateSiteWebhook(apisession, site_id=str(site_id), webhook_id=str(object_id), body=payload)
      create:
        operationId: createSiteWebhook
        function: mistapi.api.v1.sites.webhooks.createSiteWebhook(apisession, site_id=str(site_id), body=payload)

    wlans:
      update:
        operationId: updateSiteWlan
        function: mistapi.api.v1.sites.wlans.updateSiteWlan(apisession, site_id=str(site_id), wlan_id=str(object_id), body=payload)
      create:
        operationId: createSiteWlan
        function: mistapi.api.v1.sites.wlans.createSiteWlan(apisession, site_id=str(site_id), body=payload)

    wxrules:
      update:
        operationId: updateSiteWxRule
        function: mistapi.api.v1.sites.wxrules.updateSiteWxRule(apisession, site_id=str(site_id), wxrule_id=str(object_id), body=payload)
      create:
        operationId: createSiteWxRule
        function: mistapi.api.v1.sites.wxrules.createSiteWxRule(apisession, site_id=str(site_id), body=payload)

    wxtags:
      update:
        operationId: updateSiteWxTag
        function: mistapi.api.v1.sites.wxtags.updateSiteWxTag(apisession, site_id=str(site_id), wxtag_id=str(object_id), body=payload)
      create:
        operationId: createSiteWxTag
        function: mistapi.api.v1.sites.wxtags.createSiteWxTag(apisession, site_id=str(site_id), body=payload)

updateOrgConfigurationObjects:
  type: tool_consolidation
  tags: [write]
  description: |-
    Update or create configuration object for a specified org.\n
    IMPORTANT:\n
    To ensure that you are not missing any existing attributes when updating the configuration object, make sure to :\n
    1. retrieve the current configuration object using the tools `mist_get_configuration_objects` to retrieve the object defined at the site level\n
    2. Modify the desired attributes\n
    3. Use this tool to update the configuration object with the modified attributes\n
    \n
    When creating a new configuration object, make sure to use the`mist_get_configuration_object_schema` tool to discover the attributes of the configuration object and which of them are required.\n
    \n
    When deleting a WLAN Template, make sure to delete all WLANs that are using the template before deleting it, otherwise the deletion will fail\n
    When creating a WLAN, make sure to set the `template_id` attribute in the payload to the ID of an existing WLAN Template. If needed, create a new WLAN Template using this tool before creating the WLAN and use the ID of the newly created template in the WLAN payload\n
  read_only_hint: false
  destructive_hint: false
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to create or update configuration objects for
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of configuration object to create or update
      required: true
    - name: payload
      schema:
        type: object
      description: JSON payload of the configuration object to update or create. When updating an existing object, make sure to include all required attributes in the payload. It is recommended to first retrieve the current configuration object using the`mist_get_configuration_objects` tool and use the retrieved object as a base for the payload, modifying only the desired attributes
      required: true
    - name: object_id
      schema:
        type: string
        format: uuid
      description: ID of the specific configuration object to update. Optional, if not provided, a new configuration object will be created with the provided payload
  requests:
    alarmtemplates:
      update:
        operationId: updateOrgAlarmTemplate
        function: mistapi.api.v1.orgs.alarmtemplates.updateOrgAlarmTemplate(apisession, org_id=str(org_id), alarmtemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgAlarmTemplate
        function: mistapi.api.v1.orgs.alarmtemplates.createOrgAlarmTemplate(apisession, org_id=str(org_id), body=payload)

    wlans:
      update:
        operationId: updateOrgWlan
        function: mistapi.api.v1.orgs.wlans.updateOrgWlan(apisession, org_id=str(org_id), wlan_id=str(object_id), body=payload)
      create:
        operationId: createOrgWlan
        function: mistapi.api.v1.orgs.wlans.createOrgWlan(apisession, org_id=str(org_id), body=payload)

    sitegroups:
      update:
        operationId: updateOrgSiteGroup
        function: mistapi.api.v1.orgs.sitegroups.updateOrgSiteGroup(apisession, org_id=str(org_id), sitegroup_id=str(object_id), body=payload)
      create:
        operationId: createOrgSiteGroup
        function: mistapi.api.v1.orgs.sitegroups.createOrgSiteGroup(apisession, org_id=str(org_id), body=payload)

    # aptemplates:
    #   update:
    #     operationId: updateOrgAptemplate
    #     function: mistapi.api.v1.orgs.aptemplates.updateOrgAptemplate(apisession, org_id=str(org_id), aptemplate_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgAptemplate
    #     function: mistapi.api.v1.orgs.aptemplates.createOrgAptemplate(apisession, org_id=str(org_id), body=payload)

    avprofiles:
      update:
        operationId: updateOrgAntivirusProfile
        function: mistapi.api.v1.orgs.avprofiles.updateOrgAntivirusProfile(apisession, org_id=str(org_id), avprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgAntivirusProfile
        function: mistapi.api.v1.orgs.avprofiles.createOrgAntivirusProfile(apisession, org_id=str(org_id), body=payload)

    deviceprofiles:
      update:
        operationId: updateOrgDeviceProfile
        function: mistapi.api.v1.orgs.deviceprofiles.updateOrgDeviceProfile(apisession, org_id=str(org_id), deviceprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgDeviceProfile
        function: mistapi.api.v1.orgs.deviceprofiles.createOrgDeviceProfile(apisession, org_id=str(org_id), body=payload)

    # evpn_topologies:
    #   update:
    #     operationId: updateOrgEvpnTopologies
    #     function: mistapi.api.v1.orgs.evpn_topologies.updateOrgEvpnTopologies(apisession, org_id=str(org_id), evpn_topology_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgEvpnTopology
    #     function: mistapi.api.v1.orgs.evpn_topologies.createOrgEvpnTopology(apisession, org_id=str(org_id), body=payload)

    gatewaytemplates:
      update:
        operationId: updateOrgGatewayTemplate
        function: mistapi.api.v1.orgs.gatewaytemplates.updateOrgGatewayTemplate(apisession, org_id=str(org_id), gatewaytemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgGatewayTemplate
        function: mistapi.api.v1.orgs.gatewaytemplates.createOrgGatewayTemplate(apisession, org_id=str(org_id), body=payload)

    idpprofiles:
      update:
        operationId: updateOrgIdpProfile
        function: mistapi.api.v1.orgs.idpprofiles.updateOrgIdpProfile(apisession, org_id=str(org_id), idpprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgIdpProfile
        function: mistapi.api.v1.orgs.idpprofiles.createOrgIdpProfile(apisession, org_id=str(org_id), body=payload)

    aamwprofiles:
      update:
        operationId: updateOrgAAMWProfile
        function: mistapi.api.v1.orgs.aamwprofiles.updateOrgAAMWProfile(apisession, org_id=str(org_id), aamwprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgAAMWProfile
        function: mistapi.api.v1.orgs.aamwprofiles.createOrgAAMWProfile(apisession, org_id=str(org_id), body=payload)

    # mxclusters:
    #   update:
    #     operationId: updateOrgMxEdgeClusters
    #     function: mistapi.api.v1.orgs.mxclusters.updateOrgMxEdgeClusters(apisession, org_id=str(org_id), mxcluster_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxEdgeCluster
    #     function: mistapi.api.v1.orgs.mxclusters.createOrgMxEdgeCluster(apisession, org_id=str(org_id), body=payload)

    # mxedges:
    #   update:
    #     operationId: updateOrgMxEdges
    #     function: mistapi.api.v1.orgs.mxedges.updateOrgMxEdges(apisession, org_id=str(org_id), mxedge_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxEdge
    #     function: mistapi.api.v1.orgs.mxedges.createOrgMxEdge(apisession, org_id=str(org_id), body=payload)

    # mxtunnels:
    #   update:
    #     operationId: updateOrgMxTunnels
    #     function: mistapi.api.v1.orgs.mxtunnels.updateOrgMxTunnels(apisession, org_id=str(org_id), mxtunnel_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxTunnel
    #     function: mistapi.api.v1.orgs.mxtunnels.createOrgMxTunnel(apisession, org_id=str(org_id), body=payload)

    nactags:
      update:
        operationId: updateOrgNacTag
        function: mistapi.api.v1.orgs.nactags.updateOrgNacTag(apisession, org_id=str(org_id), nactag_id=str(object_id), body=payload)
      create:
        operationId: createOrgNacTag
        function: mistapi.api.v1.orgs.nactags.createOrgNacTag(apisession, org_id=str(org_id), body=payload)

    nacrules:
      update:
        operationId: updateOrgNacRule
        function: mistapi.api.v1.orgs.nacrules.updateOrgNacRule(apisession, org_id=str(org_id), nacrule_id=str(object_id), body=payload)
      create:
        operationId: createOrgNacRule
        function: mistapi.api.v1.orgs.nacrules.createOrgNacRule(apisession, org_id=str(org_id), body=payload)

    networktemplates:
      update:
        operationId: updateOrgNetworkTemplate
        function: mistapi.api.v1.orgs.networktemplates.updateOrgNetworkTemplate(apisession, org_id=str(org_id), networktemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgNetworkTemplate
        function: mistapi.api.v1.orgs.networktemplates.createOrgNetworkTemplate(apisession, org_id=str(org_id), body=payload)

    networks:
      update:
        operationId: updateOrgNetwork
        function: mistapi.api.v1.orgs.networks.updateOrgNetwork(apisession, org_id=str(org_id), network_id=str(object_id), body=payload)
      create:
        operationId: createOrgNetwork
        function: mistapi.api.v1.orgs.networks.createOrgNetwork(apisession, org_id=str(org_id), body=payload)

    psks:
      update:
        operationId: updateOrgPsk
        function: mistapi.api.v1.orgs.psks.updateOrgPsk(apisession, org_id=str(org_id), psk_id=str(object_id), body=payload)
      create:
        operationId: createOrgPsk
        function: mistapi.api.v1.orgs.psks.createOrgPsk(apisession, org_id=str(org_id), body=payload)

    rftemplates:
      update:
        operationId: updateOrgRfTemplate
        function: mistapi.api.v1.orgs.rftemplates.updateOrgRfTemplate(apisession, org_id=str(org_id), rftemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgRfTemplate
        function: mistapi.api.v1.orgs.rftemplates.createOrgRfTemplate(apisession, org_id=str(org_id), body=payload)

    services:
      update:
        operationId: updateOrgService
        function: mistapi.api.v1.orgs.services.updateOrgService(apisession, org_id=str(org_id), service_id=str(object_id), body=payload)
      create:
        operationId: createOrgService
        function: mistapi.api.v1.orgs.services.createOrgService(apisession, org_id=str(org_id), body=payload)

    servicepolicies:
      update:
        operationId: updateOrgServicePolicy
        function: mistapi.api.v1.orgs.servicepolicies.updateOrgServicePolicy(apisession, org_id=str(org_id), servicepolicy_id=str(object_id), body=payload)
      create:
        operationId: createOrgServicePolicy
        function: mistapi.api.v1.orgs.servicepolicies.createOrgServicePolicy(apisession, org_id=str(org_id), body=payload)

    # sites:
    #   update:
    #     operationId: updateOrgSites
    #     function: mistapi.api.v1.orgs.sites.updateOrgSites(apisession, org_id=str(org_id), site_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgSite
    #     function: mistapi.api.v1.orgs.sites.createOrgSite(apisession, org_id=str(org_id), body=payload)

    sitetemplates:
      update:
        operationId: updateOrgSiteTemplate
        function: mistapi.api.v1.orgs.sitetemplates.updateOrgSiteTemplate(apisession, org_id=str(org_id), sitetemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgSiteTemplate
        function: mistapi.api.v1.orgs.sitetemplates.createOrgSiteTemplate(apisession, org_id=str(org_id), body=payload)

    vpns:
      update:
        operationId: updateOrgVpn
        function: mistapi.api.v1.orgs.vpns.updateOrgVpn(apisession, org_id=str(org_id), vpn_id=str(object_id), body=payload)
      create:
        operationId: createOrgVpn
        function: mistapi.api.v1.orgs.vpns.createOrgVpn(apisession, org_id=str(org_id), body=payload)

    webhooks:
      update:
        operationId: updateOrgWebhook
        function: mistapi.api.v1.orgs.webhooks.updateOrgWebhook(apisession, org_id=str(org_id), webhook_id=str(object_id), body=payload)
      create:
        operationId: createOrgWebhook
        function: mistapi.api.v1.orgs.webhooks.createOrgWebhook(apisession, org_id=str(org_id), body=payload)

    wlantemplates:
      update:
        operationId: updateOrgTemplate
        function: mistapi.api.v1.orgs.templates.updateOrgTemplate(apisession, org_id=str(org_id), template_id=str(object_id), body=payload)
      create:
        operationId: createOrgTemplate
        function: mistapi.api.v1.orgs.templates.createOrgTemplate(apisession, org_id=str(org_id), body=payload)

    wxrules:
      update:
        operationId: updateOrgWxRule
        function: mistapi.api.v1.orgs.wxrules.updateOrgWxRule(apisession, org_id=str(org_id), wxrule_id=str(object_id), body=payload)
      create:
        operationId: createOrgWxRule
        function: mistapi.api.v1.orgs.wxrules.createOrgWxRule(apisession, org_id=str(org_id), body=payload)

    wxtags:
      update:
        operationId: updateOrgWxTag
        function: mistapi.api.v1.orgs.wxtags.updateOrgWxTag(apisession, org_id=str(org_id), wxtag_id=str(object_id), body=payload)
      create:
        operationId: createOrgWxTag
        function: mistapi.api.v1.orgs.wxtags.createOrgWxTag(apisession, org_id=str(org_id), body=payload)

changeSiteConfigurationObjects:
  type: tool_consolidation
  tags: [write_delete]
  description: |-
    Update, create or delete a configuration object for a specified site.\n
    IMPORTANT:\n
    To ensure that you are not missing any existing attributes when updating the configuration object, make sure to :\n
    1. retrieve the current configuration object using the tools `mist_get_configuration_objects` to retrieve the object defined at the site level\n
    2. Modify the desired attributes\n
    3. Use this tool to update the configuration object with the modified attributes\n
    \n
    When creating a new configuration object, make sure to use the`mist_get_configuration_object_schema` tool to discover the attributes of the configuration object and which of them are required\n
  read_only_hint: false
  destructive_hint: true
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to create/update/delete configuration objects for
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of configuration object to create, update, or delete
      required: true
    - name: payload
      schema:
        type: object
      description: JSON payload of the configuration object to update or create. Required when action_type is 'create' or 'update'. When updating an existing object, make sure to include all required attributes in the payload. It is recommended to first retrieve the current configuration object using the`mist_get_configuration_objects` tool and use the retrieved object as a base for the payload, modifying only the desired attributes
      required: true
    - name: object_id
      schema:
        type: string
        format: uuid
      description: ID of the specific configuration object to update or delete. Required when action_type is 'update' or 'delete'
      required: false
  requests:
    devices:
      update:
        operationId: updateSiteDevice
        function: mistapi.api.v1.sites.devices.updateSiteDevice(apisession, site_id=str(site_id), device_id=str(object_id), body=payload)

    evpn_topologies:
      update:
        operationId: updateSiteEvpnTopologies
        function: mistapi.api.v1.sites.evpn_topologies.updateSiteEvpnTopology(apisession, site_id=str(site_id), evpn_topology_id=str(object_id), body=payload)
      create:
        operationId: createSiteEvpnTopology
        function: mistapi.api.v1.sites.evpn_topologies.createSiteEvpnTopology(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSiteEvpnTopology
        function: mistapi.api.v1.sites.evpn_topologies.deleteSiteEvpnTopology(apisession, site_id=str(site_id), evpn_topology_id=str(object_id))

    # mxedges:
    #   update:
    #     operationId: updateSiteMxEdge
    #     function: mistapi.api.v1.sites.mxedges.updateSiteMxEdge(apisession, site_id=str(site_id), mxedge_id=str(object_id), body=payload)
    #   create:
    #     operationId: createSiteMxEdge
    #     function: mistapi.api.v1.sites.mxedges.createSiteMxEdge(apisession, site_id=str(site_id), body=payload)

    psks:
      update:
        operationId: updateSitePsk
        function: mistapi.api.v1.sites.psks.updateSitePsk(apisession, site_id=str(site_id), psk_id=str(object_id), body=payload)
      create:
        operationId: createSitePsk
        function: mistapi.api.v1.sites.psks.createSitePsk(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSitePsk
        function: mistapi.api.v1.sites.psks.deleteSitePsk(apisession, site_id=str(site_id), psk_id=str(object_id))

    webhooks:
      update:
        operationId: updateSiteWebhook
        function: mistapi.api.v1.sites.webhooks.updateSiteWebhook(apisession, site_id=str(site_id), webhook_id=str(object_id), body=payload)
      create:
        operationId: createSiteWebhook
        function: mistapi.api.v1.sites.webhooks.createSiteWebhook(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSiteWebhook
        function: mistapi.api.v1.sites.webhooks.deleteSiteWebhook(apisession, site_id=str(site_id), webhook_id=str(object_id))

    wlans:
      update:
        operationId: updateSiteWlan
        function: mistapi.api.v1.sites.wlans.updateSiteWlan(apisession, site_id=str(site_id), wlan_id=str(object_id), body=payload)
      create:
        operationId: createSiteWlan
        function: mistapi.api.v1.sites.wlans.createSiteWlan(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSiteWlan
        function: mistapi.api.v1.sites.wlans.deleteSiteWlan(apisession, site_id=str(site_id), wlan_id=str(object_id))

    wxrules:
      update:
        operationId: updateSiteWxRule
        function: mistapi.api.v1.sites.wxrules.updateSiteWxRule(apisession, site_id=str(site_id), wxrule_id=str(object_id), body=payload)
      create:
        operationId: createSiteWxRule
        function: mistapi.api.v1.sites.wxrules.createSiteWxRule(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSiteWxRule
        function: mistapi.api.v1.sites.wxrules.deleteSiteWxRule(apisession, site_id=str(site_id), wxrule_id=str(object_id))

    wxtags:
      update:
        operationId: updateSiteWxTag
        function: mistapi.api.v1.sites.wxtags.updateSiteWxTag(apisession, site_id=str(site_id), wxtag_id=str(object_id), body=payload)
      create:
        operationId: createSiteWxTag
        function: mistapi.api.v1.sites.wxtags.createSiteWxTag(apisession, site_id=str(site_id), body=payload)
      delete:
        operationId: deleteSiteWxTag
        function: mistapi.api.v1.sites.wxtags.deleteSiteWxTag(apisession, site_id=str(site_id), wxtag_id=str(object_id))

changeOrgConfigurationObjects:
  type: tool_consolidation
  tags: [write_delete]
  description: |-
    Update, create or delete configuration object for a specified org.\n
    IMPORTANT:\n
    To ensure that you are not missing any existing attributes when updating the configuration object, make sure to :\n
    1. retrieve the current configuration object using the tools `mist_get_configuration_objects` to retrieve the object defined at the site level\n
    2. Modify the desired attributes\n
    3. Use this tool to update the configuration object with the modified attributes\n
    \n
    When creating a new configuration object, make sure to use the`mist_get_configuration_object_schema` tool to discover the attributes of the configuration object and which of them are required.\n
    \n
    When deleting a WLAN Template, make sure to delete all WLANs that are using the template before deleting it, otherwise the deletion will fail\n
    When creating a WLAN, make sure to set the `template_id` attribute in the payload to the ID of an existing WLAN Template. If needed, create a new WLAN Template using this tool before creating the WLAN and use the ID of the newly created template in the WLAN payload\n
  read_only_hint: false
  destructive_hint: true
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to create, update or delete configuration objects for
      required: true
    - name: object_type
      schema:
        type: string
      description: Type of configuration object to create, update, or delete
      required: true
    - name: payload
      schema:
        type: object
      description: JSON payload of the configuration object to update or create. When updating an existing object, make sure to include all required attributes in the payload. It is recommended to first retrieve the current configuration object using the`mist_get_configuration_objects` tool and use the retrieved object as a base for the payload, modifying only the desired attributes
      required: true
    - name: object_id
      schema:
        type: string
        format: uuid
      description: ID of the specific configuration object to update. Required when action_type is 'update' or 'delete'
  requests:
    alarmtemplates:
      update:
        operationId: updateOrgAlarmTemplate
        function: mistapi.api.v1.orgs.alarmtemplates.updateOrgAlarmTemplate(apisession, org_id=str(org_id), alarmtemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgAlarmTemplate
        function: mistapi.api.v1.orgs.alarmtemplates.createOrgAlarmTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgAlarmTemplate
        function: mistapi.api.v1.orgs.alarmtemplates.deleteOrgAlarmTemplate(apisession, org_id=str(org_id), alarmtemplate_id=str(object_id))

    wlans:
      update:
        operationId: updateOrgWlan
        function: mistapi.api.v1.orgs.wlans.updateOrgWlan(apisession, org_id=str(org_id), wlan_id=str(object_id), body=payload)
      create:
        operationId: createOrgWlan
        function: mistapi.api.v1.orgs.wlans.createOrgWlan(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgWlan
        function: mistapi.api.v1.orgs.wlans.deleteOrgWlan(apisession, org_id=str(org_id), wlan_id=str(object_id))

    sitegroups:
      update:
        operationId: updateOrgSiteGroup
        function: mistapi.api.v1.orgs.sitegroups.updateOrgSiteGroup(apisession, org_id=str(org_id), sitegroup_id=str(object_id), body=payload)
      create:
        operationId: createOrgSiteGroup
        function: mistapi.api.v1.orgs.sitegroups.createOrgSiteGroup(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgSiteGroup
        function: mistapi.api.v1.orgs.sitegroups.deleteOrgSiteGroup(apisession, org_id=str(org_id), sitegroup_id=str(object_id))

    # aptemplates:
    #   update:
    #     operationId: updateOrgAptemplate
    #     function: mistapi.api.v1.orgs.aptemplates.updateOrgAptemplate(apisession, org_id=str(org_id), aptemplate_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgAptemplate
    #     function: mistapi.api.v1.orgs.aptemplates.createOrgAptemplate(apisession, org_id=str(org_id), body=payload)

    avprofiles:
      update:
        operationId: updateOrgAntivirusProfile
        function: mistapi.api.v1.orgs.avprofiles.updateOrgAntivirusProfile(apisession, org_id=str(org_id), avprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgAntivirusProfile
        function: mistapi.api.v1.orgs.avprofiles.createOrgAntivirusProfile(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgAntivirusProfile
        function: mistapi.api.v1.orgs.avprofiles.deleteOrgAntivirusProfile(apisession, org_id=str(org_id), avprofile_id=str(object_id))

    deviceprofiles:
      update:
        operationId: updateOrgDeviceProfile
        function: mistapi.api.v1.orgs.deviceprofiles.updateOrgDeviceProfile(apisession, org_id=str(org_id), deviceprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgDeviceProfile
        function: mistapi.api.v1.orgs.deviceprofiles.createOrgDeviceProfile(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgDeviceProfile
        function: mistapi.api.v1.orgs.deviceprofiles.deleteOrgDeviceProfile(apisession, org_id=str(org_id), deviceprofile_id=str(object_id))

    # evpn_topologies:
    #   update:
    #     operationId: updateOrgEvpnTopologies
    #     function: mistapi.api.v1.orgs.evpn_topologies.updateOrgEvpnTopologies(apisession, org_id=str(org_id), evpn_topology_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgEvpnTopology
    #     function: mistapi.api.v1.orgs.evpn_topologies.createOrgEvpnTopology(apisession, org_id=str(org_id), body=payload)

    gatewaytemplates:
      update:
        operationId: updateOrgGatewayTemplate
        function: mistapi.api.v1.orgs.gatewaytemplates.updateOrgGatewayTemplate(apisession, org_id=str(org_id), gatewaytemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgGatewayTemplate
        function: mistapi.api.v1.orgs.gatewaytemplates.createOrgGatewayTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgGatewayTemplate
        function: mistapi.api.v1.orgs.gatewaytemplates.deleteOrgGatewayTemplate(apisession, org_id=str(org_id), gatewaytemplate_id=str(object_id))

    idpprofiles:
      update:
        operationId: updateOrgIdpProfile
        function: mistapi.api.v1.orgs.idpprofiles.updateOrgIdpProfile(apisession, org_id=str(org_id), idpprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgIdpProfile
        function: mistapi.api.v1.orgs.idpprofiles.createOrgIdpProfile(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgIdpProfile
        function: mistapi.api.v1.orgs.idpprofiles.deleteOrgIdpProfile(apisession, org_id=str(org_id), idpprofile_id=str(object_id))

    aamwprofiles:
      update:
        operationId: updateOrgAAMWProfile
        function: mistapi.api.v1.orgs.aamwprofiles.updateOrgAAMWProfile(apisession, org_id=str(org_id), aamwprofile_id=str(object_id), body=payload)
      create:
        operationId: createOrgAAMWProfile
        function: mistapi.api.v1.orgs.aamwprofiles.createOrgAAMWProfile(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgAAMWProfile
        function: mistapi.api.v1.orgs.aamwprofiles.deleteOrgAAMWProfile(apisession, org_id=str(org_id), aamwprofile_id=str(object_id))

    # mxclusters:
    #   update:
    #     operationId: updateOrgMxEdgeClusters
    #     function: mistapi.api.v1.orgs.mxclusters.updateOrgMxEdgeClusters(apisession, org_id=str(org_id), mxcluster_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxEdgeCluster
    #     function: mistapi.api.v1.orgs.mxclusters.createOrgMxEdgeCluster(apisession, org_id=str(org_id), body=payload)

    # mxedges:
    #   update:
    #     operationId: updateOrgMxEdges
    #     function: mistapi.api.v1.orgs.mxedges.updateOrgMxEdges(apisession, org_id=str(org_id), mxedge_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxEdge
    #     function: mistapi.api.v1.orgs.mxedges.createOrgMxEdge(apisession, org_id=str(org_id), body=payload)

    # mxtunnels:
    #   update:
    #     operationId: updateOrgMxTunnels
    #     function: mistapi.api.v1.orgs.mxtunnels.updateOrgMxTunnels(apisession, org_id=str(org_id), mxtunnel_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgMxTunnel
    #     function: mistapi.api.v1.orgs.mxtunnels.createOrgMxTunnel(apisession, org_id=str(org_id), body=payload)

    nactags:
      update:
        operationId: updateOrgNacTag
        function: mistapi.api.v1.orgs.nactags.updateOrgNacTag(apisession, org_id=str(org_id), nactag_id=str(object_id), body=payload)
      create:
        operationId: createOrgNacTag
        function: mistapi.api.v1.orgs.nactags.createOrgNacTag(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgNacTag
        function: mistapi.api.v1.orgs.nactags.deleteOrgNacTag(apisession, org_id=str(org_id), nactag_id=str(object_id))

    nacrules:
      update:
        operationId: updateOrgNacRule
        function: mistapi.api.v1.orgs.nacrules.updateOrgNacRule(apisession, org_id=str(org_id), nacrule_id=str(object_id), body=payload)
      create:
        operationId: createOrgNacRule
        function: mistapi.api.v1.orgs.nacrules.createOrgNacRule(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgNacRule
        function: mistapi.api.v1.orgs.nacrules.deleteOrgNacRule(apisession, org_id=str(org_id), nacrule_id=str(object_id))

    networktemplates:
      update:
        operationId: updateOrgNetworkTemplate
        function: mistapi.api.v1.orgs.networktemplates.updateOrgNetworkTemplate(apisession, org_id=str(org_id), networktemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgNetworkTemplate
        function: mistapi.api.v1.orgs.networktemplates.createOrgNetworkTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgNetworkTemplate
        function: mistapi.api.v1.orgs.networktemplates.deleteOrgNetworkTemplate(apisession, org_id=str(org_id), networktemplate_id=str(object_id))

    networks:
      update:
        operationId: updateOrgNetwork
        function: mistapi.api.v1.orgs.networks.updateOrgNetwork(apisession, org_id=str(org_id), network_id=str(object_id), body=payload)
      create:
        operationId: createOrgNetwork
        function: mistapi.api.v1.orgs.networks.createOrgNetwork(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgNetwork
        function: mistapi.api.v1.orgs.networks.deleteOrgNetwork(apisession, org_id=str(org_id), network_id=str(object_id))

    psks:
      update:
        operationId: updateOrgPsk
        function: mistapi.api.v1.orgs.psks.updateOrgPsk(apisession, org_id=str(org_id), psk_id=str(object_id), body=payload)
      create:
        operationId: createOrgPsk
        function: mistapi.api.v1.orgs.psks.createOrgPsk(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgPsk
        function: mistapi.api.v1.orgs.psks.deleteOrgPsk(apisession, org_id=str(org_id), psk_id=str(object_id))

    rftemplates:
      update:
        operationId: updateOrgRfTemplate
        function: mistapi.api.v1.orgs.rftemplates.updateOrgRfTemplate(apisession, org_id=str(org_id), rftemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgRfTemplate
        function: mistapi.api.v1.orgs.rftemplates.createOrgRfTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgRfTemplate
        function: mistapi.api.v1.orgs.rftemplates.deleteOrgRfTemplate(apisession, org_id=str(org_id), rftemplate_id=str(object_id))

    services:
      update:
        operationId: updateOrgService
        function: mistapi.api.v1.orgs.services.updateOrgService(apisession, org_id=str(org_id), service_id=str(object_id), body=payload)
      create:
        operationId: createOrgService
        function: mistapi.api.v1.orgs.services.createOrgService(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgService
        function: mistapi.api.v1.orgs.services.deleteOrgService(apisession, org_id=str(org_id), service_id=str(object_id))

    servicepolicies:
      update:
        operationId: updateOrgServicePolicy
        function: mistapi.api.v1.orgs.servicepolicies.updateOrgServicePolicy(apisession, org_id=str(org_id), servicepolicy_id=str(object_id), body=payload)
      create:
        operationId: createOrgServicePolicy
        function: mistapi.api.v1.orgs.servicepolicies.createOrgServicePolicy(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgServicePolicy
        function: mistapi.api.v1.orgs.servicepolicies.deleteOrgServicePolicy(apisession, org_id=str(org_id), servicepolicy_id=str(object_id))

    # sites:
    #   update:
    #     operationId: updateOrgSites
    #     function: mistapi.api.v1.orgs.sites.updateOrgSites(apisession, org_id=str(org_id), site_id=str(object_id), body=payload)
    #   create:
    #     operationId: createOrgSite
    #     function: mistapi.api.v1.orgs.sites.createOrgSite(apisession, org_id=str(org_id), body=payload)

    sitetemplates:
      update:
        operationId: updateOrgSiteTemplate
        function: mistapi.api.v1.orgs.sitetemplates.updateOrgSiteTemplate(apisession, org_id=str(org_id), sitetemplate_id=str(object_id), body=payload)
      create:
        operationId: createOrgSiteTemplate
        function: mistapi.api.v1.orgs.sitetemplates.createOrgSiteTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgSiteTemplate
        function: mistapi.api.v1.orgs.sitetemplates.deleteOrgSiteTemplate(apisession, org_id=str(org_id), sitetemplate_id=str(object_id))

    vpns:
      update:
        operationId: updateOrgVpn
        function: mistapi.api.v1.orgs.vpns.updateOrgVpn(apisession, org_id=str(org_id), vpn_id=str(object_id), body=payload)
      create:
        operationId: createOrgVpn
        function: mistapi.api.v1.orgs.vpns.createOrgVpn(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgVpn
        function: mistapi.api.v1.orgs.vpns.deleteOrgVpn(apisession, org_id=str(org_id), vpn_id=str(object_id))

    webhooks:
      update:
        operationId: updateOrgWebhook
        function: mistapi.api.v1.orgs.webhooks.updateOrgWebhook(apisession, org_id=str(org_id), webhook_id=str(object_id), body=payload)
      create:
        operationId: createOrgWebhook
        function: mistapi.api.v1.orgs.webhooks.createOrgWebhook(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgWebhook
        function: mistapi.api.v1.orgs.webhooks.deleteOrgWebhook(apisession, org_id=str(org_id), webhook_id=str(object_id))

    wlantemplates:
      update:
        operationId: updateOrgTemplate
        function: mistapi.api.v1.orgs.templates.updateOrgTemplate(apisession, org_id=str(org_id), template_id=str(object_id), body=payload)
      create:
        operationId: createOrgTemplate
        function: mistapi.api.v1.orgs.templates.createOrgTemplate(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgTemplate
        function: mistapi.api.v1.orgs.templates.deleteOrgTemplate(apisession, org_id=str(org_id), template_id=str(object_id))

    wxrules:
      update:
        operationId: updateOrgWxRule
        function: mistapi.api.v1.orgs.wxrules.updateOrgWxRule(apisession, org_id=str(org_id), wxrule_id=str(object_id), body=payload)
      create:
        operationId: createOrgWxRule
        function: mistapi.api.v1.orgs.wxrules.createOrgWxRule(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgWxRule
        function: mistapi.api.v1.orgs.wxrules.deleteOrgWxRule(apisession, org_id=str(org_id), wxrule_id=str(object_id))

    wxtags:
      update:
        operationId: updateOrgWxTag
        function: mistapi.api.v1.orgs.wxtags.updateOrgWxTag(apisession, org_id=str(org_id), wxtag_id=str(object_id), body=payload)
      create:
        operationId: createOrgWxTag
        function: mistapi.api.v1.orgs.wxtags.createOrgWxTag(apisession, org_id=str(org_id), body=payload)
      delete:
        operationId: deleteOrgWxTag
        function: mistapi.api.v1.orgs.wxtags.deleteOrgWxTag(apisession, org_id=str(org_id), wxtag_id=str(object_id))

getSelf:
  type: tool_consolidation
  tags: [self_account]
  description: |-
    This tool can be used to retrieve information about the current user and account
    The information provided will depend on the `action_type` attribute:
    * `account_info`: will return information about the account including account ID, account name, and the list of orgs (and their respective `org_id`) the account has access to, with the permissions level (read or write) for each org
    * `api_usage`: will return information about the API usage of the account including the number of API calls made in the current hour cycle and the API call limit for the account
    * `login_failures`: will return information about the recent login failures for the account including the timestamp of the failure, the source IP address, and the reason for the failure
  read_only_hint: true
  destructive_hint: false
  match_name: action_type
  parameters:
    - name: action_type
      schema:
        type: string
        enum: [account_info, api_usage, login_failures]
      description: Type of information to retrieve about the current user and account. Possible values are `account_info`, `api_usage`, and `login_failures`
      required: true
  requests:
    account_info:
      get:
        operationId: getSelf
        function: mistapi.api.v1.self.self.getSelf(apisession)
    api_usage:
      get:
        operationId: getSelfApiUsage
        function: mistapi.api.v1.self.usage.getSelfApiUsage(apisession)
    login_failures:
      get:
        operationId: getSelfLoginFailures
        function: mistapi.api.v1.self.login_failures.getSelfLoginFailures(apisession)

getSiteRrmInfo:
  type: tool_consolidation
  tags: [sites_rrm]
  description: Retrieve Radio Resource Management (RRM) information for a site. Use current_channel_planning to get the current channel plan, current_rrm_considerations to get RRM considerations for a specific device and band, current_rrm_neighbors to list current RRM neighbor APs for a band, or events to list RRM change events over a time range.
  read_only_hint: true
  destructive_hint: false
  match_name: rrm_info_type
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID
      required: true
    - name: rrm_info_type
      schema:
        type: string
        enum:
          [
            current_channel_planning,
            current_rrm_considerations,
            current_rrm_neighbors,
            events,
          ]
      description: "Type of RRM information to retrieve: current_channel_planning returns the current channel plan for the site; current_rrm_considerations returns per-AP RRM considerations (requires device_id and band); current_rrm_neighbors lists current RRM neighbor APs for a band (requires band); events lists RRM change events over a time range"
      required: true
    - name: device_id
      schema:
        type: string
        format: uuid
      description: ID of the AP to retrieve RRM considerations for. Required when rrm_info_type is current_rrm_considerations
      required: false
      required_if:
        rrm_info_type:
          - current_rrm_considerations
    - name: band
      schema:
        type: string
        enum: ["24", "5", "6"]
      description: 802.11 band. Required when rrm_info_type is current_rrm_considerations or current_rrm_neighbors
      required: false
      required_if:
        rrm_info_type:
          - current_rrm_considerations
          - current_rrm_neighbors
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: duration
      schema:
        type: string
      description: Time range duration (e.g. 1d, 1h, 10m). Only applicable when rrm_info_type is events
      required: false
      only_if:
        rrm_info_type:
          - events
    - name: limit
      schema:
        type: integer
        default: 200
      description: Max number of results per page. Only applicable when rrm_info_type is events
      required: false
      only_if:
        rrm_info_type:
          - events
    - name: page
      schema:
        type: integer
        default: 1
      description: Page number for pagination. Only applicable when rrm_info_type is events
      required: false
      only_if:
        rrm_info_type:
          - events
  requests:
    current_channel_planning:
      get:
        operationId: getSiteCurrentChannelPlanning
        function: mistapi.api.v1.sites.rrm.getSiteCurrentChannelPlanning(apisession, site_id=str(site_id))
    current_rrm_considerations:
      get:
        operationId: getSiteCurrentRrmConsiderations
        function: mistapi.api.v1.sites.rrm.getSiteCurrentRrmConsiderations(apisession, site_id=str(site_id), device_id=str(device_id), band=str(band.value))
    current_rrm_neighbors:
      get:
        operationId: listSiteCurrentRrmNeighbors
        function: mistapi.api.v1.sites.rrm.listSiteCurrentRrmNeighbors(apisession, site_id=str(site_id), band=str(band.value))
    events:
      list:
        operationId: listSiteRrmEvents
        function: mistapi.api.v1.sites.rrm.listSiteRrmEvents(apisession, site_id=str(site_id), band=band.value if band else None, start=str(start) if start else None, end=str(end) if end else None, duration=duration if duration else None, limit=limit, page=page)

getOrgLicenses:
  type: tool_consolidation
  tags: [orgs]
  description: |-
    This tool can be used to retrieve information about the licenses of an org
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to retrieve license information for
      required: true
    - name: response_type
      schema:
        type: string
      description: Type of license information to retrieve. `claim_status` returns the status of an asynchronous license claim operation, `by_site` returns the list of licenses for each site in the org, and `summary` returns a summary of the licenses in the org including total count and count by license type
      required: true
  match_name: response_type
  requests:
    claim_status:
      get:
        operationId: GetOrgLicenseAsyncClaimStatus
        function: mistapi.api.v1.orgs.claim.GetOrgLicenseAsyncClaimStatus(apisession, org_id=str(org_id))
    by_site:
      get:
        operationId: getOrgLicensesBySite
        function: mistapi.api.v1.orgs.licenses.getOrgLicensesBySite(apisession, org_id=str(org_id))
    summary:
      get:
        operationId: getOrgLicensesSummary
        function: mistapi.api.v1.orgs.licenses.getOrgLicensesSummary(apisession, org_id=str(org_id))

getStats:
  type: tool_consolidation
  tags: [stats]
  description: Use this tool to retrieve various statistics from Mist infrastructure including organization and site-level data. Supports stats for MxEdges, devices, BGP, OSPF, peer paths, ports, and wireless clients. Use object_id to filter results by device ID or MAC address (format varies by stats_type).
  read_only_hint: true
  destructive_hint: false
  match_name: stats_type
  parameters:
    - name: stats_type
      schema:
        type: string
        enum:
          [
            org,
            sites,
            org_mxedges,
            org_wireless_clients,
            org_devices,
            org_bgp,
            org_ospf,
            org_peer_paths,
            org_ports,
            site_mxedges,
            site_wireless_clients,
            site_devices,
            site_bgp,
            site_ospf,
            site_ports,
          ]
      description: "Type of statistics to retrieve: org, sites, org_mxedges, org_wireless_clients, org_devices, org_bgp, org_ospf, org_peer_paths, org_ports, site_mxedges, site_wireless_clients, site_devices, site_bgp, site_ospf, or site_ports"
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: Organization ID (required for all queries)
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: "Site ID (required for site-level stats: site_mxedges, site_wireless_clients, site_devices, site_bgp, site_ospf, site_ports)"
      required: false
      required_if:
        stats_type:
          - site_mxedges
          - site_wireless_clients
          - site_devices
          - site_bgp
          - site_ospf
          - site_ports
    - name: device_type
      schema:
        type: string
        enum: [ap, switch, gateway]
      description: "Device type filter (ap, switch, gateway). Only applicable when stats_type is org_devices or site_devices"
      required: false
      only_if:
        stats_type:
          - org_devices
          - site_devices
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: object_id
      schema:
        type: string
        format: uuid
      description: "Filter by specific object ID or MAC address (format depends on stats_type): Mist Edge ID for mxedges, device ID for devices, MAC address for BGP/OSPF/peer_paths/ports, client MAC for wireless_clients, site ID for sites"
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    org:
      get:
        operationId: getOrgStats
        function: mistapi.api.v1.orgs.stats.getOrgStats(apisession, org_id=str(org_id), start=str(start), end=str(end) )
    sites:
      list:
        operationId: listOrgSiteStats
        function: mistapi.api.v1.orgs.stats.listOrgSiteStats(apisession, org_id=str(org_id), start=str(start), end=str(end), limit=limit)
      get:
        operationId: getSiteStats
        function: mistapi.api.v1.sites.stats.getSiteStats(apisession, site_id=str(object_id),)
    org_mxedges:
      list:
        operationId: listOrgMxEdgesStats
        function: mistapi.api.v1.orgs.stats.listOrgMxEdgesStats(apisession, org_id=str(org_id), start=str(start), end=str(end), limit=limit)
      get:
        operationId: getOrgMxEdgeStats
        function: mistapi.api.v1.orgs.stats.getOrgMxEdgeStats(apisession, org_id=str(org_id), mxedge_id=str(object_id) )
    org_devices:
      list:
        operationId: listOrgDevicesStats
        function: mistapi.api.v1.orgs.stats.listOrgDevicesStats(apisession, org_id=str(org_id), type=device_type.value if device_type else None, start=str(start), end=str(end), mac=str(object_id), limit=limit)
    org_bgp:
      list:
        operationId: searchOrgBgpStats
        function: mistapi.api.v1.orgs.stats.searchOrgBgpStats(apisession, org_id=str(org_id), start=str(start), end=str(end), mac=str(object_id), limit=limit)
    org_ospf:
      list:
        operationId: searchOrgOspfStats
        function: mistapi.api.v1.orgs.stats.searchOrgOspfStats(apisession, org_id=str(org_id), start=str(start), end=str(end), mac=str(object_id)  )
    org_peer_paths:
      list:
        operationId: searchOrgPeerPathStats
        function: mistapi.api.v1.orgs.stats.searchOrgPeerPathStats(apisession, org_id=str(org_id), start=str(start), end=str(end), mac=str(object_id), limit=limit)
    org_ports:
      list:
        operationId: searchOrgSwOrGwPorts
        function: mistapi.api.v1.orgs.stats.searchOrgSwOrGwPorts(apisession, org_id=str(org_id), mac=str(object_id), limit=limit)
    site_mxedges:
      list:
        operationId: listSiteMxEdgesStats
        function: mistapi.api.v1.sites.stats.listSiteMxEdgesStats(apisession, site_id=str(site_id), start=str(start), end=str(end), limit=limit)
      get:
        operationId: getSiteMxEdgeStats
        function: mistapi.api.v1.sites.stats.getSiteMxEdgeStats(apisession, site_id=str(site_id), start=str(start), end=str(end), mxedge_id=str(object_id) )
    site_wireless_clients:
      list:
        operationId: listSiteWirelessClientsStats
        function: mistapi.api.v1.sites.stats.listSiteWirelessClientsStats(apisession, site_id=str(site_id), start=str(start), end=str(end), limit=limit)
      get:
        operationId: getSiteWirelessClientStats
        function: mistapi.api.v1.sites.stats.getSiteWirelessClientStats(apisession, site_id=str(site_id),  client_mac=str(object_id)  )
    site_devices:
      list:
        operationId: listSiteDevicesStats
        function: mistapi.api.v1.sites.stats.listSiteDevicesStats(apisession, site_id=str(site_id), type=device_type.value if device_type else None, limit=limit)
      get:
        operationId: getSiteDeviceStats
        function: mistapi.api.v1.sites.stats.getSiteDeviceStats(apisession, site_id=str(site_id), device_id=str(object_id)  )
    site_bgp:
      list:
        operationId: searchSiteBgpStats
        function: mistapi.api.v1.sites.stats.searchSiteBgpStats(apisession, site_id=str(site_id), start=str(start), end=str(end), mac=str(object_id), limit=limit)
    site_ospf:
      list:
        operationId: searchSiteOspfStats
        function: mistapi.api.v1.sites.stats.searchSiteOspfStats(apisession, site_id=str(site_id), start=str(start), end=str(end), mac=str(object_id), limit=limit)
    site_ports:
      list:
        operationId: searchSiteSwOrGwPorts
        function: mistapi.api.v1.sites.stats.searchSiteSwOrGwPorts(apisession, site_id=str(site_id), mac=str(object_id), limit=limit)

searchEvents:
  type: tool_consolidation
  tags: [events]
  description: |-
    Search for events across an organization or site with flexible filtering options.

    This tool queries events from various sources including devices, MX Edge instances, and clients. You can:
    - Filter by time range using `start` and `end` (epoch seconds)
    - Filter by event type (use `mist_get_constants` tool first to discover available event types)
    - Apply source-specific filters (MAC address, text search, SSID, etc.)

    IMPORTANT: Always specify an `event_type` to limit results. Use `mist_get_constants` with:
    - `object_type=device_events` for device events
    - `object_type=mxedge_events` for MX Edge events  
    - `object_type=client_events` for WAN/wireless client events
    - `object_type=nac_events` for NAC client events
  read_only_hint: true
  destructive_hint: false
  match_name: event_source
  parameters:
    - name: event_source
      schema:
        type: string
        enum:
          [
            device,
            mxedge,
            wan_client,
            wireless_client,
            nac_client,
            roaming,
            rogue,
          ]
      description: "Event source type: device, mxedge, wan_client, wireless_client, nac_client, roaming (requires site_id), or rogue (requires site_id)"
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to search for events in
      required: true
    - name: event_type
      schema:
        type: string
      description: Comma-separated event types to filter by. The list of possible event types can be obtained with the `mist_get_constants` tool with `object_type=device_events` when `event_source` is `device`, `object_type=mxedge_events` when `event_source` is `mxedge`, `object_type=client_events` when `event_source` is `wan_client` or `wireless_client`, `object_type=nac_events` when `event_source` is `nac_client`
      required: false
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID (required for roaming/rogue events, optional for others)
      required: false
      required_if:
        event_source:
          - roaming
          - rogue
    - name: mac
      schema:
        type: string
      description: MAC address to filter by (device/WAN client/NAC client/rogue events only)
      required: false
    - name: text
      schema:
        type: string
      description: Text search in event details (device/NAC client events only)
      required: false
      only_if:
        event_source:
          - device
          - nac_client
    - name: ssid
      schema:
        type: string
      description: SSID filter (wireless_client/nac_client/rogue events only)
      required: false
      only_if:
        event_source:
          - wireless_client
          - nac_client
          - rogue
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    device:
      site_id:
        operationId: searchSiteDeviceEvents
        function: mistapi.api.v1.sites.devices.searchSiteDeviceEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None,  mac=str(mac) if mac else None, text=str(text) if text else None, limit=limit)
      org_id:
        operationId: searchOrgDeviceEvents
        function: mistapi.api.v1.orgs.devices.searchOrgDeviceEvents(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mac=str(mac) if mac else None, text=str(text) if text else None, limit=limit)
    mxedge:
      site_id:
        operationId: mxedges.searchSiteMistEdgeEvents
        function: mistapi.api.v1.sites.mxedges.searchSiteMistEdgeEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mxedge_id=f"00000000-0000-0000-1000-{str(mac)}" if mac else None, limit=limit)
      org_id:
        operationId: searchOrgMistEdgeEvents
        function: mistapi.api.v1.orgs.mxedges.searchOrgMistEdgeEvents(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mxedge_id=f"00000000-0000-0000-1000-{str(mac)}" if mac else None, limit=limit)
    wan_client:
      site_id:
        operationId: searchSiteWanClientEvents
        function: mistapi.api.v1.sites.wan_clients.searchSiteWanClientEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mac=str(mac) if mac else None, limit=limit)
      org_id:
        operationId: searchOrgWanClientEvents
        function: mistapi.api.v1.orgs.wan_clients.searchOrgWanClientEvents(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mac=str(mac) if mac else None, limit=limit)
    wireless_client:
      site_id:
        operationId: searchSiteWirelessClientEvents
        function: mistapi.api.v1.sites.clients.searchSiteWirelessClientEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, ssid=str(ssid) if ssid else None, limit=limit)
      org_id:
        operationId: searchOrgWirelessClientEvents
        function: mistapi.api.v1.orgs.clients.searchOrgWirelessClientEvents(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, ssid=str(ssid) if ssid else None, limit=limit)
    nac_client:
      site_id:
        operationId: searchSiteNacClientEvents
        function: mistapi.api.v1.sites.nac_clients.searchSiteNacClientEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mac=str(mac) if mac else None, text=str(text) if text else None, ssid=str(ssid) if ssid else None, limit=limit)
      org_id:
        operationId: searchOrgNacClientEvents
        function: mistapi.api.v1.orgs.nac_clients.searchOrgNacClientEvents(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, mac=str(mac) if mac else None, text=str(text) if text else None, ssid=str(ssid) if ssid else None, limit=limit)
    roaming:
      site_id:
        operationId: listSiteRoamingEvents
        function: mistapi.api.v1.sites.events.listSiteRoamingEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, limit=limit)
    rogue:
      site_id:
        operationId: searchSiteRogueEvents
        function: mistapi.api.v1.sites.rogues.searchSiteRogueEvents(apisession, site_id=str(site_id), start=str(start) if start else None, end=str(end) if end else None, type=str(event_type) if event_type else None, ssid=str(ssid) if ssid else None, ap_mac=str(mac) if mac else None, limit=limit)

# searchDevices:
#   type: tool_consolidation
#   tags: [devices]
#   description: |-
#     This tool can be used to search for devices in an organization or site.
#     You can filter the search by device type, device name, or MAC address using the `device_type`, `name`, and `mac` parameters, respectively
#     IMPORTANT: this tool only returns devices that are currently connected to Mist
#   read_only_hint: true
#   destructive_hint: false
#   parameters:
#     - name: scope
#       schema:
#         type: string
#         enum: [org, site]
#       description: Whether to search for devices in the entire organization or a specific site. If `site` is selected, the `site_id` parameter is required
#       required: true
#     - name: org_id
#       schema:
#         type: string
#         format: uuid
#       description: ID of the organization to search for devices in
#       required: true
#     - name: site_id
#       schema:
#         type: string
#         format: uuid
#       description: ID of the site to search for devices in
#       required: false
#       required_if:
#         scope:
#           - site
#     - name: device_type
#       schema:
#         type: string
#         enum: [ap, switch, gateway]
#       description: Type of device to search for
#       required: false
#     - name: hostname
#       schema:
#         type: string
#       description: Hostname of the device to search for. Supports partial matches
#       required: false
#     - name: mac
#       schema:
#         type: string
#       description: MAC address of the device to search for
#       required: false
#     - name: model
#       schema:
#         type: string
#       description: Model of the device to search for
#       required: false
#     - name: limit
#       schema:
#         type: integer
#         default: 20
#       description: Max number of results to return (max 1000)
#       required: false
#   match_name: scope
#   requests:
#     org:
#       list:
#         operationId: searchOrgDevices
#         function: mistapi.api.v1.orgs.devices.searchOrgDevices(apisession, org_id=str(org_id), type=str(device_type) if device_type else None, hostname=str(hostname) if hostname else None, mac=str(mac) if mac else None, model=str(model) if model else None, limit=limit )
#     site:
#       list:
#         operationId: searchSiteDevices
#         function: mistapi.api.v1.sites.devices.searchSiteDevices(apisession, site_id=str(site_id), type=str(device_type) if device_type else None, hostname=str(hostname) if hostname else None, mac=str(mac) if mac else None, model=str(model) if model else None, limit=limit )

searchGuestAuthorization:
  type: tool_consolidation
  tags: [clients]
  description: |-
    Search for guest authorization entries in an organization or site
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: scope
      schema:
        type: string
        enum: [org, site]
      description: Whether to search in the entire organization or a specific site. If `site` is selected, the `site_id` parameter is required
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to search for guest authorization entries in
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to search for guest authorization entries in
      required: false
      required_if:
        scope:
          - site
    - name: guest_mac
      schema:
        type: string
      description: MAC address of the guest to search for in the authorization entries
      required: false
    - name: wlan_id
      schema:
        type: string
        format: uuid
      description: ID of the WLAN to filter guest authorization entries by
      required: false
    - name: auth_method
      schema:
        type: string
      description: Authentication method to filter guest authorization entries by
      required: false
    - name: ssid
      schema:
        type: string
      description: SSID to filter guest authorization entries by
      required: false
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  match_name: scope
  if_filter: guest_mac
  requests:
    org:
      list:
        operationId: searchOrgGuestAuthorization
        function: mistapi.api.v1.orgs.guests.searchOrgGuestAuthorization(apisession,org_id=str(org_id),wlan_id=str(wlan_id) if wlan_id else None,auth_method=auth_method if auth_method else None,ssid=ssid if ssid else None,start=str(start) if start else None,end=str(end) if end else None, limit=limit )
      get:
        operationId: getOrgGuestAuthorization
        function: mistapi.api.v1.orgs.guests.getOrgGuestAuthorization(apisession, org_id=str(org_id), guest_mac=str(guest_mac) )
    site:
      list:
        operationId: searchSiteGuestAuthorization
        function: mistapi.api.v1.sites.guests.searchSiteGuestAuthorization(apisession, site_id=str(site_id), wlan_id=str(wlan_id) if wlan_id else None, auth_method=auth_method if auth_method else None, ssid=ssid if ssid else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )
      get:
        operationId: getSiteGuestAuthorization
        function: mistapi.api.v1.sites.guests.getSiteGuestAuthorization(apisession, site_id=str(site_id), guest_mac=str(guest_mac) )

getOrgOrSiteInfo:
  type: tool_consolidation
  tags: [info]
  description: |-
    Search information about the organizations or sites
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: info_type
      schema:
        type: string
        enum: [org, site]
      description: Type of information to search for. Possible values are `org` and `site`
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to search for information in
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to search for information in
      required: false
  match_name: info_type
  if_filter: site_id
  requests:
    org:
      get:
        operationId: getOrg
        function: mistapi.api.v1.orgs.orgs.getOrg(apisession, org_id=str(org_id))
    site:
      list:
        operationId: listOrgSites
        function: mistapi.api.v1.orgs.sites.listOrgSites(apisession, org_id=str(org_id))
      get:
        operationId: getSiteInfo
        function: mistapi.api.v1.sites.sites.getSiteInfo(apisession, site_id=str(site_id) )

searchAuditLogs:
  type: tool_consolidation
  tags: [events]
  description: |-
    Search audit logs for the current account or an organization
  read_only_hint: true
  destructive_hint: false
  match_name: scope
  parameters:
    - name: scope
      schema:
        type: string
        enum: [self, org]
      description: Whether to retrieve audit logs for the account or a specific organization. If `org` is selected, the `org_id` parameter is required
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to retrieve audit logs for
      required: false
      required_if:
        scope:
          - org
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: message
      schema:
        type: string
      description: Message to filter audit logs by (partial search)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    self:
      list:
        operationId: listSelfAuditLogs
        function: mistapi.api.v1.self.logs.listSelfAuditLogs(apisession, start=str(start) if start else None, end=str(end) if end else None, message=str(message) if message else None, limit=limit )
    org:
      list:
        operationId: listOrgAuditLogs
        function: mistapi.api.v1.orgs.logs.listOrgAuditLogs(apisession, org_id=str(org_id), start=str(start) if start else None, end=str(end) if end else None, message=str(message) if message else None, limit=limit )

searchClient:
  type: tool_consolidation
  tags: [clients]
  description: |-
    Search for clients across an organization or specific site. 
    Supports searching by client type (WAN, wired, wireless, NAC), MAC address, hostname, IP address, and more.
    Use wildcards (*) for partial matches on MAC address, hostname, IP, and text fields.
    Different client types support different filter parameters - the tool will validate compatibility.
  read_only_hint: true
  destructive_hint: false
  match_name: client_type
  parameters:
    - name: client_type
      schema:
        type: string
        enum: [wired, wireless, wan, nac]
      description: "Type of client: WAN, wired, wireless, or NAC"
      required: true
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to search for clients in
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID for site-level search (optional)
      required: false
    - name: device_mac
      schema:
        type: string
      description: MAC address of the access point or switch. Not applicable for WAN or NAC clients
      required: false
    - name: band
      schema:
        type: string
        enum: ["24", "5", "6"]
      description: 802.11 band (24 or 5 or 6 GHz). Wireless clients only
      required: false
      only_if:
        client_type:
          - wireless
    - name: ssid
      schema:
        type: string
      description: SSID name. Wireless or NAC clients only
      required: false
      only_if:
        client_type:
          - wireless
          - nac
    - name: mac
      schema:
        type: string
      description: Client MAC address (supports * wildcard for partial match)
      required: false
    - name: hostname
      schema:
        type: string
      description: Client hostname (supports * wildcard). Not applicable for WAN or wired clients
      required: false
    - name: ip
      schema:
        type: string
      description: Client IP address (supports * wildcard). Not applicable for NAC clients
      required: false
    - name: text
      schema:
        type: string
      description: Free text search in client details (supports * wildcard). Not applicable for WAN clients
      required: false
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    wan:
      list:
        operationId: searchOrgWanClients
        function: mistapi.api.v1.orgs.wan_clients.searchOrgWanClients(apisession, org_id=str(org_id), site_id=str(site_id) if site_id else None, mac=str(mac) if mac else None, hostname=str(hostname) if hostname else None, ip=str(ip) if ip else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )
    wired:
      list:
        operationId: searchOrgWiredClients
        function: mistapi.api.v1.orgs.wired_clients.searchOrgWiredClients(apisession, org_id=str(org_id), site_id=str(site_id) if site_id else None, device_mac=str(device_mac) if device_mac else None, mac=str(mac) if mac else None, ip=str(ip) if ip else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )
    wireless:
      list:
        operationId: searchOrgWirelessClients
        function: mistapi.api.v1.orgs.clients.searchOrgWirelessClients(apisession, org_id=str(org_id), site_id=str(site_id) if site_id else None, ap=str(device_mac) if device_mac else None, band=str(band) if band else None, ssid=str(ssid) if ssid else None, mac=str(mac) if mac else None, hostname=str(hostname) if hostname else None, ip=str(ip) if ip else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )
    nac:
      list:
        operationId: searchOrgNacClients
        function: mistapi.api.v1.orgs.nac_clients.searchOrgNacClients(apisession, org_id=str(org_id), site_id=str(site_id) if site_id else None, ssid=str(ssid) if ssid else None, mac=str(mac) if mac else None, hostname=str(hostname) if hostname else None, text=str(text) if text else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )

listRogueDevices:
  type: tool_consolidation
  tags: [sites_rogues]
  description: |-
    Retrieve a list of rogue devices (APs or clients) for a site, with optional filters for rogue AP type and time range
  read_only_hint: true
  destructive_hint: false
  match_name: rogue_type
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: ID of the site to retrieve rogue devices for
      required: true
    - name: rogue_type
      schema:
        type: string
        enum: [ap, client]
      description: Type of rogue device to filter by
      required: true
    # - name: rogue_bssid
    #   schema:
    #     type: string
    #   description: BSSID of the rogue device to filter by. Only applicable when filtering for rogue APs
    #   required: false
    - name: rogue_ap_type
      schema:
        type: string
        enum: [honeypot, lan, others, spoof]
      description: Type of rogue AP to filter by. Only applicable when filtering for rogue APs
      required: false
      only_if:
        rogue_type:
          - ap
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    ap:
      list:
        operationId: listSiteRogueAPs
        function: mistapi.api.v1.sites.insights.listSiteRogueAPs(apisession, site_id=str(site_id), type=rogue_ap_type.value if rogue_ap_type else None, limit=limit, start=str(start) if start else None, end=str(end) if end else None )
      # get:
      #   operationId: getSiteRogueAP
      #   function: mistapi.api.v1.sites.rogues.getSiteRogueAP(apisession, site_id=str(site_id), rogue_bssid=rogue_bssid)
    client:
      list:
        operationId: listSiteRogueClients
        function: mistapi.api.v1.sites.insights.listSiteRogueClients(apisession, site_id=str(site_id), limit=limit, start=str(start) if start else None, end=str(end) if end else None )

listSiteSleInfo:
  type: tool_consolidation
  tags: [sles]
  description: List SLE metadata for a site scope. Use metrics to list available SLE metrics for a given scope, or classifiers to list the classifiers available for a specific metric.
  read_only_hint: true
  destructive_hint: false
  match_name: query_type
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID
      required: true
    - name: query_type
      schema:
        type: string
        enum: [metrics, classifiers]
      description: "Type of metadata to retrieve: metrics returns the list of available SLE metrics for the given scope; classifiers returns the list of classifiers for a specific metric (requires metric parameter)"
      required: true
    - name: scope
      schema:
        type: string
        enum: [ap, client, gateway, site, switch]
      description: "Scope of the SLE data: site, ap, client, gateway, or switch"
      required: true
    - name: scope_id
      schema:
        type: string
      description: "ID of the scoped object: `site_id` if `scope=site`; `device_id` if `scope=ap`, `switch`, or `gateway`; `MAC address` if `scope=client`"
      required: true
    - name: metric
      schema:
        type: string
      description: SLE metric name to retrieve classifiers for. Required when query_type is classifiers. Use query_type=metrics first to discover available metric names
      required: false
      required_if:
        test:
          - classifiers
  requests:
    metrics:
      list:
        operationId: listSiteSlesMetrics
        function: mistapi.api.v1.sites.sle.listSiteSlesMetrics(apisession, site_id=str(site_id), scope=scope.value, scope_id=scope_id)
    classifiers:
      list:
        operationId: listSiteSleMetricClassifiers
        function: mistapi.api.v1.sites.sle.listSiteSleMetricClassifiers(apisession, site_id=str(site_id), scope=scope.value, scope_id=scope_id, metric=metric)

searchAlarms:
  type: tool_consolidation
  tags: [events]
  description: |-
    Search for raised alarms in an organization or site with optional filtering. 
      
    Scopes:
    - `org`: Search all alarms across the organization
    - `site`: Search alarms in a specific site (requires `site_id`)
    - `suppressed`: View temporarily disabled alarms across the organization

    Alarm groups: `infrastructure` (network device/connectivity issues), `marvis` (AI-driven network detections), `security` (security events)

    Common Marvis alarm types: `bad_cable`, `bad_wan_uplink`, `dns_failure`, `arp_failure`, `auth_failure`, `dhcp_failure`, `missing_vlan`, `negotiation_mismatch`, `port_flap`

    For a complete list of alarm types, use `mist_get_constants` with `object_type=alarm_definitions`.
  read_only_hint: true
  destructive_hint: false
  match_name: scope
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: Organization ID
      required: true
    - name: scope
      schema:
        type: string
        enum: [org, site, suppressed]
      description: "Search scope: `org` (organization-wide), `site` (specific site, requires site_id), or `suppressed` (disabled alarms)"
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID. Can be used to filter alarms for a specific site. Required if `scope` is set to `site`
      required: false
      required_if:
        scope:
          - site
    - name: group
      schema:
        type: string
      description: "Only for org/site scope. Alarm group. enum: `infrastructure`, `marvis`, `security`.  The `marvis` group is used to retrieve AI-driven network issue detections."
      only_if:
        scope:
          - org
          - site
    - name: severity
      schema:
        type: string
      description: "Only for org/site scope.Severity of the alarm. enum: `critical`, `major`, `minor`, `warn`, `info`"
      only_if:
        scope:
          - org
          - site
    - name: alarm_type
      schema:
        type: string
      description: "Only for org/site scope. Comma separated list of types of the alarm (e.g., 'bad_cable,auth_failure'). IMPORTANT: use the `mist_get_constants` tool with `object_type=alarm_definitions`to get the list of possible alarm types"
      only_if:
        scope:
          - org
          - site
    - name: acked
      schema:
        type: boolean
      description: Only for org/site scope. Whether to filter for acknowledged (true) or unacknowledged (false) alarms
      only_if:
        scope:
          - org
          - site
    - name: start
      schema:
        type: integer
      description: Only for org/site scope. Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: Only for org/site scope. End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Only for org/site scope. Max number of results to return (max 1000)
      required: false
  requests:
    org:
      list:
        operationId: searchOrgAlarms
        function: mistapi.api.v1.orgs.alarms.searchOrgAlarms(apisession,org_id=str(org_id),group=group if group else None,severity=severity if severity else None,type=alarm_type if alarm_type else None,acked=acked if acked else None,start=str(start) if start else None,end=str(end) if end else None,limit=limit,)
    site:
      list:
        operationId: searchSiteAlarms
        function: mistapi.api.v1.sites.alarms.searchSiteAlarms(apisession,site_id=str(site_id),group=group if group else None,severity=severity if severity else None,type=alarm_type if alarm_type else None,acked=acked if acked else None,start=str(start) if start else None,end=str(end) if end else None,limit=limit,)
    suppressed:
      list:
        operationId: listOrgSuppressedAlarms
        function: mistapi.api.v1.orgs.alarmtemplates.listOrgSuppressedAlarms(apisession, org_id=str(org_id))

searchDeviceConfigHistory:
  type: tool_consolidation
  tags: [configuration]
  description: |-
    Search for entries in device config history. 
    This tool can be used to track configuration changes over time, useful for troubleshooting issues that started after a config change.
  read_only_hint: true
  destructive_hint: false
  match_name: query_type
  parameters:
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Site ID of the device to search config history for
      required: true
    - name: query_type
      schema:
        type: string
        enum: [history, last_configs]
      description: Whether to search for config history entries or just retrieve the last config entry for each device
      required: true
    - name: device_type
      schema:
        type: string
        enum: [ap, switch, gateway]
      description: Type of device to search config history for
      required: true
    - name: device_mac
      schema:
        type: string
      description: MAC address of the device to search config history for
      required: false
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    history:
      list:
        operationId: searchSiteDeviceConfigHistory
        function: mistapi.api.v1.sites.devices.searchSiteDeviceConfigHistory(apisession, site_id=str(site_id), type=device_type.value if device_type else None, mac=str(device_mac) if device_mac else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )
    last_configs:
      list:
        operationId: searchSiteDeviceLastConfigs
        function: mistapi.api.v1.sites.devices.searchSiteDeviceLastConfigs(apisession, site_id=str(site_id), device_type=device_type.value if device_type else None, mac=str(device_mac) if device_mac else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit)

searchNacUserMacs:
  type: tool_consolidation
  tags: [orgs_nac]
  description: |-
    Search for NAC user MAC addresses in an organization or site, with optional filters for associated SSID and time range. User MACs are used to perform MAC Authentication with Juniper Mist NAC.
  read_only_hint: true
  destructive_hint: false
  if_filter: usermac_id
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: Organization ID to search for NAC endpoints in
      required: true
    - name: usermac_id
      schema:
        type: string
      description: ID of the User MAC address to return details for. If specified, other filters are ignored and details for the specified User MAC address is returned if it exists
      required: false
    - name: mac
      schema:
        type: string
      description: Partial/full MAC address of the NAC endpoint to search for
      required: false
    - name: labels
      schema:
        type: array
        items:
          type: string
      description: Comma separated list of labels to filter NAC endpoints by. A NAC endpoint must have all the specified labels to be included in the results
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    default:
      list:
        operationId: searchOrgUserMacs
        function: mistapi.api.v1.orgs.usermacs.searchOrgUserMacs(apisession, org_id=str(org_id), mac=str(mac) if mac else None, labels=labels if labels else None, limit=limit )
      get:
        operationId: getOrgUserMac
        function: mistapi.api.v1.orgs.usermacs.getOrgUserMac(apisession, org_id=str(org_id), usermac_id=str(usermac_id) )

getOrgSle:
  type: tool_consolidation
  tags: [sles]
  description: Get Org SLEs (all/worst sites, Mx Edges, ...). Use the `mist_get_insight_metrics` tool to get the list of available SLE metrics
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: Organization ID
      required: true
    - name: metric
      schema:
        type: string
      description: Metric to look at. Use the `mist_get_insight_metrics` tool to get the list of available SLE metrics
      required: true
    - name: sle
      schema:
        type: string
      description: Type of SLE data to retrieve for the organization sites. Use the `mist_get_insight_metrics` tool to get the list of available SLE metrics
      required: false
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    default:
      get:
        operationId: getOrgSle
        function: mistapi.api.v1.orgs.insights.getOrgSle(apisession, org_id=str(org_id), metric=str(metric), sle=str(sle) if sle else None, start=str(start) if start else None, end=str(end) if end else None, limit=limit )

getOrgSitesSle:
  type: tool_consolidation
  tags: [sles]
  description: Get SLE summary for the organization sites.
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: Organization ID
      required: true
    - name: sle
      schema:
        type: string
        enum: [wifi, wired, wan]
      description: Type of SLE data to retrieve for the sites. Possible values are `wifi`, `wired`, and `wan`
      required: true
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
    - name: limit
      schema:
        type: integer
        default: 20
      description: Max number of results to return (max 1000)
      required: false
  requests:
    default:
      get:
        operationId: getOrgSitesSle
        function: mistapi.api.v1.orgs.insights.getOrgSitesSle(apisession, org_id=str(org_id), sle=sle.value, start=str(start) if start else None, end=str(end) if end else None, limit=limit )

troubleshoot:
  type: tool_consolidation
  tags: [marvis]
  description: |-
    Troubleshoot sites, devices, clients, and wired clients for maximum of last 7 days from current time. Use the `mist_search_client` tool to find a client MAC Address. Use the `mist_search_device` tool to find device MAC Address. **NOTE**: requires Marvis subscription license
  read_only_hint: true
  destructive_hint: false
  parameters:
    - name: org_id
      schema:
        type: string
        format: uuid
      description: ID of the organization to run the troubleshooting query for
      required: true
    - name: site_id
      schema:
        type: string
        format: uuid
      description: Used to troubleshoot a whole site. ID of the site to run the troubleshooting query for
      required: false
    - name: mac
      schema:
        type: string
      description: Used to troubleshoot a specific client or device. MAC address of the client or device to run the troubleshooting query for. Not required if troubleshooting a whole site with `site_id`
      required: false
    - name: troubleshoot_type
      schema:
        type: string
        enum: [wan, wired, wireless]
      description: Type of troubleshooting query to run. Possible values are `wan`, `wired`, and `wireless`. If `wan` is selected, the query will troubleshoot the WAN. If `wired` is selected, the query will troubleshoot the wired network. If `wireless` is selected, the query will troubleshoot the wireless network.
      required: true
    - name: start
      schema:
        type: integer
      description: Start time (epoch timestamp in seconds)
      required: false
    - name: end
      schema:
        type: integer
      description: End time (epoch timestamp in seconds)
      required: false
  requests:
    default:
      get:
        operationId: troubleshootOrg
        function: mistapi.api.v1.orgs.troubleshoot.troubleshootOrg(apisession, org_id=str(org_id), site_id=str(site_id) if site_id else None, type=troubleshoot_type.value, mac=str(mac) if mac else None, start=str(start) if start else None, end=str(end) if end else None )
